import { InStoragePipe } from './in-storage.pipe';

describe('InStoragePipe', () => {
  it('create an instance', () => {
    const pipe = new InStoragePipe();
    expect(pipe).toBeTruthy();
  });
});
