import { ScoreIconPipe } from './score-icon.pipe';

describe('ScoreIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreIconPipe();
    expect(pipe).toBeTruthy();
  });
});
